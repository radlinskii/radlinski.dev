---
import ThemeSelect from "./ThemeSelect.astro";
import { SITE_NAME } from "../consts";
---

<header>
    <nav>
        <h2><a href="/">{SITE_NAME}</a></h2>
        <div class="nav-panel">
            <a href="/posts"><span class="no-underline">/</span>posts</a>
            <ThemeSelect />
        </div>
    </nav>
</header>

<style>
    header {
        margin: 0;
        padding: 1rem 2rem;
        display: flex;
        justify-content: center;
        background: var(--bg-color1);
        color: var(--fg-color1);
    }

    nav {
        width: 1280px;
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    h2 {
        margin: 0;
        font-size: 1.2rem;
    }

    nav a {
        color: var(--fg-color1);
        text-decoration: none;
    }

    nav a:hover {
        color: var(--accent2);
        text-decoration: underline;
    }

    .nav-panel {
        display: flex;
        gap: 2rem;
    }

    .link-prefix {
        margin-right: -2px;
    }

    @media (width < 1024px) {
        header {
            padding: 0.5rem 1rem;
        }
    }
</style>
